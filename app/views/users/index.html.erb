<% provide :title, t("title.all_users") %>
<h1><%= t "title.all_users" %></h1>
<ul class="users" >
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name, user %>
    </li>
  <% end %>
</ul>
